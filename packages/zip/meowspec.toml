# meowspec.toml — zip
[package]
name        = "zip"
version     = "3.0"
release     = 1
summary     = "Create and modify zip archives"
description = "Create and modify zip archives"
license     = "Info-ZIP"
homepage    = "http://www.info-zip.org/"
arch        = "x86_64"
source_url  = "https://downloads.sourceforge.net/infozip/zip30.tar.gz"
sha256      = ""  # fill in after: sha256sum $(basename https://downloads.sourceforge.net/infozip/zip30.tar.gz)

depends = [
  "musl",
]
build_depends = [
  "gcc",
  "make",
]
provides  = []
conflicts = []
