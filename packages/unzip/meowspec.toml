# meowspec.toml — unzip
[package]
name        = "unzip"
version     = "6.0"
release     = 1
summary     = "Extract and examine zip archives"
description = "Extract and examine zip archives"
license     = "Info-ZIP"
homepage    = "http://www.info-zip.org/"
arch        = "x86_64"
source_url  = "https://downloads.sourceforge.net/infozip/unzip60.tar.gz"
sha256      = ""  # fill in after: sha256sum $(basename https://downloads.sourceforge.net/infozip/unzip60.tar.gz)

depends = [
  "musl",
]
build_depends = [
  "gcc",
  "make",
]
provides  = []
conflicts = []
