# meowspec.toml — dhcpcd
[package]
name        = "dhcpcd"
version     = "10.0.8"
release     = 1
summary     = "DHCP client daemon"
description = "DHCP client daemon"
license     = "BSD-2-Clause"
homepage    = "https://github.com/NetworkConfiguration/dhcpcd"
arch        = "x86_64"
source_url  = "https://github.com/NetworkConfiguration/dhcpcd/releases/download/v10.0.8/dhcpcd-10.0.8.tar.xz"
sha256      = ""  # fill in after: sha256sum $(basename https://github.com/NetworkConfiguration/dhcpcd/releases/download/v10.0.8/dhcpcd-10.0.8.tar.xz)

depends = [
  "musl",
]
build_depends = [
  "gcc",
  "make",
]
provides  = []
conflicts = []
