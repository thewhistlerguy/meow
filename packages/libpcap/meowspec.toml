# meowspec.toml — libpcap
[package]
name        = "libpcap"
version     = "1.10.4"
release     = 1
summary     = "Packet capture library"
description = "Packet capture library"
license     = "BSD-3-Clause"
homepage    = "https://www.tcpdump.org/"
arch        = "x86_64"
source_url  = "https://www.tcpdump.org/release/libpcap-1.10.4.tar.gz"
sha256      = ""  # fill in after: sha256sum $(basename https://www.tcpdump.org/release/libpcap-1.10.4.tar.gz)

depends = [
  "musl",
]
build_depends = [
  "gcc",
  "make",
]
provides  = []
conflicts = []
