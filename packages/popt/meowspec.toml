# meowspec.toml — popt
[package]
name        = "popt"
version     = "1.19"
release     = 1
summary     = "C library for parsing command line parameters"
description = "C library for parsing command line parameters"
license     = "MIT"
homepage    = "https://github.com/rpm-software-management/popt"
arch        = "x86_64"
source_url  = "https://ftp.osuosl.org/pub/rpm/popt/releases/popt-1.x/popt-1.19.tar.gz"
sha256      = ""  # fill in after: sha256sum $(basename https://ftp.osuosl.org/pub/rpm/popt/releases/popt-1.x/popt-1.19.tar.gz)

depends = [
  "musl",
]
build_depends = [
  "gcc",
  "make",
]
provides  = []
conflicts = []
